munera_area = {
	ability = adm
	
	allow_multiple = yes

	potential = {
		num_locations > 0 
	}

	allow = {
		any_area = {
			#any_location_in_area = {
			#	integration_level = core
			#	count = all
			#}
		}
	}

	select_trigger = {
		looking_for_a = area
		source = actor
		target_flag = target
		name = "develop_province_select_province"
		none_available_msg_key = "develop_province_no_provinces"
		column = {
			data = name
		}
		column = {
			data = development
		}
		column = {
			data = population
		}
		visible = {
			NOT = { cabinet_already_performing_same_task_on_target = { cabinet_action = munera_area interaction_target = target } }
		}
		enabled = {
			#any_location_in_area = {
			#	integration_level = core
			#	count = all
			#}
		}
		map_color = {
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH
				factor = {
					value = development
					divide = 100
				}
			}
		}
	}

	location_modifier = {
		local_unrest = -0.025
		local_monthly_prosperity = 0.0025
	}



	map_marker = {
		show_for_owner = yes
	}
	

	ai_will_do = {
		
	}

}



bread_and_games = {
	ability = adm
	
	allow_multiple = yes

	potential = {
		num_locations > 0 
	}

	allow = {
		any_area = {
			#any_location_in_area = {
			#	integration_level = core
			#	count = all
			#}
		}
	}

	select_trigger = {
		looking_for_a = area
		source = actor
		target_flag = target
		name = "develop_province_select_province"
		none_available_msg_key = "develop_province_no_provinces"
		column = {
			data = name
		}
		column = {
			data = development
		}
		column = {
			data = population
		}
		visible = {
			NOT = { cabinet_already_performing_same_task_on_target = { cabinet_action = munera_area interaction_target = target } }
		}
		enabled = {
			#any_location_in_area = {
			#	integration_level = core
			#	count = all
			#}
		}
		map_color = {
			lerp = {
				min_color = define:NMapColors|MAP_COLOR_LOW
				max_color = define:NMapColors|MAP_COLOR_HIGH
				factor = {
					value = development
					divide = 100
				}
			}
		}
	}
	country_modifier ={
		tax_income_efficiency = -0.05

	}
	
	location_modifier = {
		local_population_growth = 0.002
		local_unrest = -0.02
	}


	map_marker = {
		show_for_owner = yes
	}
	

	ai_will_do = {
		
	}
}
