# Light infantry: units with barely any armor. Usually ranged attackers too, though not guaranteed to be in the early ages
# Heavy infantry: robust units in heavy armor. Usually melee attackers
# Light cavalry: mounted units which strength is speed and which use barely any armor. Good for flanking
# Heavy cavalry: mounted units which breach frontal into the enemy rows and can withstand a lot of punishment. Become obsolete with Age of Revolutions
# Artillery: currently all the cannons and mortar units which are used for siege and combat alike
# Auxiliary: currently all the supply units which ensure that your troops do not starve

a_1_heavy_infantry = {
	default = yes
	copy_from = a_age_0_diadochi_heavy_infantry
	
	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}
	
	# upgrades_to = a_men_at_arms

	
	gfx_tags = { medium_tag }
}
a_1_spearmen = {
	default = yes
	copy_from = a_age_0_diadochi_spearmen
	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}
	
	# upgrades_to = a_men_at_arms

	maintenance_demand = spearmen_maintenance
	construction_demand = spearmen_construction
	gfx_tags = { medium_tag }
}

a_1_light_infantry = {
	default = yes
	copy_from = a_age_0_diadochi_light_infantry
	

	# upgrades_to = a_crossbowmen

	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	
	gfx_tags = { archer_tag medium_tag }
}
a_1_ranged_infantry = {
	default = yes
	copy_from = a_age_0_diadochi_ranged_infantry
	

	# upgrades_to = a_crossbowmen

	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = peasants
		multiply = 0.05
	}
	maintenance_demand = ranged_infantry_maintenance
	construction_demand = ranged_infantry_construction
	gfx_tags = { archer_tag medium_tag }
}
a_1_heavy_cavalry = {
	copy_from = a_age_0_diadochi_heavy_cavalry
	
	
	country_potential = { 
		government_type != government_type:steppe_horde
	}

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = laborers
		multiply = 0.1
	}
	
	# upgrades_to = a_men_at_arms

	
	gfx_tags = { medium_tag }
}
a_1_light_cavalry = {
	copy_from = a_age_0_diadochi_light_cavalry
	

	# upgrades_to = 

	mercenaries_per_location = {
		pop_type = nobles
		multiply = 0.3
	}
	
	mercenaries_per_location = {
		pop_type = tribesmen
		multiply = 0.01
	}
	

	gfx_tags = { archer_tag medium_tag }
}

a_1_heavy_siege = {
	copy_from = a_age_0_diadochi_heavy_siege

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}
	
	#upgrades_to = a_falconet
}

a_1_battle_artillery = {
	copy_from = a_age_0_diadochi_battle_artillery

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}
	
	#upgrades_to = a_falconet
}

a_1_siege_unit = {
	copy_from = a_age_0_diadochi_siege

	#root = location. Checks location_potential first so don't need that scripting in
	mercenaries_per_location = {
		pop_type = burghers
		multiply = 0.05
	}
	
	#upgrades_to = a_falconet
}

a_diadochi_light_levy = {
	copy_from = a_1_light_infantry

	buildable = no
	levy = yes
	gfx_tags = { light_tag }
}

a_diadochi_light_cavalry_levy = {
	copy_from = a_1_light_cavalry

	buildable = no
	levy = yes
	gfx_tags = { light_tag }
}

a_villagers_levy = {
	copy_from = a_1_heavy_infantry

	buildable = no
	levy = yes
	gfx_tags = { light_tag }
}
